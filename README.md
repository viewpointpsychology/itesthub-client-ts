# iTestHub API Client - Typescript

API Client for iTestHub built using https://github.com/hey-api/openapi-ts

You can build your own client using, e.g.:
```
npx @hey-api/openapi-ts -i itesthub-api.yaml -o src
```
